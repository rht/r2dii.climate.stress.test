# with missing `asset_type` errors gracefully

    argument "asset_type" is missing, with no default

# with multiple values of some argumeht errors gracefully

    Input arguments to stress test run need to be of length 1

